/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/fix-my-budget', // replace 'repo-name' with the name of your repository
    assetPrefix: '/fix-my-budget/', // replace 'repo-name' with the name of your repository
  
};

export default nextConfig;
